/**
 * Main entry point for the Scorekeeper application
 */
const PlayerManager = require('./managers/PlayerManager');
const Player = require('./models/Player');

module.exports = {
  PlayerManager,
  Player
};
